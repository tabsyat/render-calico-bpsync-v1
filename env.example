# Copy to .env for local testing only. NEVER commit a real .env file.
# On Render, set these as Environment Variables in the service dashboard instead.

RENDER_URL=https://your-render-instance.onrender.com/api/v1/tournaments/your-slug
RENDER_TOKEN=your-render-api-token
CALICO_URL=https://your-calico-instance.calicotab.com/api/v1/tournaments/your-slug
CALICO_TOKEN=your-calico-api-token
